enum Days {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
}

const d1 = Days.Monday

export enum HttpStatusCode {
    "OKAY" = 200,
    "NOT_FOUND" = 404
}

const er1 = HttpStatusCode.OKAY

// console.log(er1)